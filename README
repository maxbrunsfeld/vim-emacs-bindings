This plugin adds to vim the basic navigation and editing key bindings from emacs (shared by bash and cocoa text views). The bindings are added to insert and command-line modes, and in the prompt window for [Command-T](http://www.vim.org/scripts/script.php?script_id=3025).

